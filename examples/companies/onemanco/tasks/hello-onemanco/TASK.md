---
schema: agentcompanies/v1
kind: task
name: Hello, OneManCo
slug: hello-onemanco
assignee: builder
---

# Hello, OneManCo

A starter task to exercise the end-to-end OpenHuman × Paperclip loop.

Create a file `HELLO.md` in the workspace containing a one-paragraph summary of what
OneManCo is and how a heartbeat flows from Paperclip into the OpenHuman harness. Post a
comment linking the work, then mark the task `done`.

This task's only purpose is to prove the `openhuman_local` adapter executes a real
heartbeat: checkout → work → comment → status update.
